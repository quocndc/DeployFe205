import { LocateIcon, Home, LaughIcon, LayersIcon, LucideContact, LucideIcon, Tags } from 'lucide-react'

export const Icons: { [key: string]: LucideIcon } = {
  home: Home,
  tags: Tags,
  logo: LocateIcon,
  list: LayersIcon,
  contact: LucideContact,
  policy: LaughIcon,
}
